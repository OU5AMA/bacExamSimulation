server:
  port: 8080

spring:
  jackson:
    serialization:
      indent_output: true

  # JWT configuration
  jwt:
    secret-key: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
    expiration: 86400000 # 24 hours
    refresh-token:
      expiration: 604800000 # 7 days

  # Logging
  logging:
    level:
      org.springframework: INFO
      com.bacExam: DEBUG

  # (optional) CORS _ in case you're connecting to frontend later
  cors:
    allowed-origins: http://localhost:5173
    allowed-methods: GET,POST,PUT,DELETE
    allowed-headers: "*"

## Datasource for h2
  datasource:
    url: jdbc:h2:mem:school_db
    username: root
    password: toor
    driver-class-name: org.h2.Driver

  # database configuration
  h2:
    console:
      enabled: true
      path: /h2-console # access it at http://localhost:8080/h2-console

  jpa:
    hibernate:
      ddl-auto: update # use 'create' or 'create-drop' for full reset during dev
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.H2Dialect


########################### PRODUCTION CODE ##############################

## Datasource for postgreSQL
#  datasource:
#    url: jdbc:postgresql://localhost:5432/school_db
#    username: root
#    password: toor
#    driver-class-name: org.postfresql.Driver

#  jpa:
#    hibernate:
#      ddl-auto: update # Change to 'validate' or 'none' in production
#    show-sql: true
#    properties:
#      hibernate:
#        format_sql: true
#        dialect: org.hibernate.dialect.PostgreSQLDialect